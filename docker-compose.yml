version: "3.9"
services:
  web:
    build: .
    ports:
      - "80:4000"
    logging:
      driver: "json-file"
      options: 
        max-size: "200k"
    volumes:
      - .:/code
  redis:
    image: "redis:alpine"
